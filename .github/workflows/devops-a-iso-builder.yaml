name: devops a - iso builder

on:
  workflow_dispatch:
    inputs:
      update-stable:
        description: "Update iso-manifest-stable.json to latest template manifest"
        required: true
        default: "yes"

permissions:
  contents: write 
  actions: read

concurrency:
  group: devops-${{ github.ref }}
  cancel-in-progress: true

jobs:
  iso-builder:
    timeout-minutes: 20
    environment: lab
    runs-on:
      - self-hosted
      - Linux
      - X64

    env:
      ## required for proxmox host access
      PVE_ACCESS_HOST:   ${{ secrets.PVE_ACCESS_HOST }}
      PM_TOKEN_ID:       ${{ secrets.PM_TOKEN_ID }}
      PM_TOKEN_SECRET:   ${{ secrets.PM_TOKEN_SECRET }}
      PVE_NODE:          ${{ secrets.PVE_NODE }}
      PVE_STORAGE_VM:    ${{ secrets.PVE_STORAGE_VM }}
      PVE_ISO_STORAGE:   ${{ secrets.PVE_ISO_STORAGE }} 
      ## disable doppler in makefile
      CI: "true"
      DOPPLER: "0"

    steps:
      ## log local runner information
      - name: Identify runner
        run: |
          echo "Runner: $RUNNER_NAME"
          echo "Host: $(hostname)"
          echo "User: $(whoami)"

      ## prepare the local runner environment
      - name: Checkout
        uses: actions/checkout@v4

      ## get a baseline arch iso profile established
      - name: Bootstrap ArchISO profile
        run: |
          set -euo pipefail

          BUILD_ROOT=".iso-build/archiso"
          mkdir -p "$BUILD_ROOT"

          HOST_UID=$(id -u)
          HOST_GID=$(id -g)

          docker run --rm -i --privileged \
            -v "$PWD:/work" -w /work \
            -e HOST_UID="$HOST_UID" -e HOST_GID="$HOST_GID" \
            archlinux:latest bash -lc '
              set -eux
              pacman -Syu --noconfirm archiso
              rm -rf .iso-build/archiso/profile
              cp -r /usr/share/archiso/configs/releng .iso-build/archiso/profile
              chown -R "$HOST_UID:$HOST_GID" .iso-build/archiso/profile
            '

          ls -R .iso-build/archiso/profile

      - name: Show default automated_script.sh
        run: |
          echo "=== Default releng .automated_script.sh ==="
          cat .iso-build/archiso/profile/airootfs/root/.automated_script.sh
          echo "=== END ==="

      ## copy in custom automated script to configure the iso
      - name: Install custom automated_script.sh
        run: |
          set -euo pipefail

          SRC="infra/arch/iso/automated_script.sh"
          DEST=".iso-build/archiso/profile/airootfs/root/.automated_script.sh"

          cp "$SRC" "$DEST"
          chmod +x "$DEST"

          echo "=== Installed custom automated_script.sh ==="
          ls -l "$DEST"

      - name: Show customized automated_script.sh
        run: |
          echo "=== Default releng .automated_script.sh ==="
          cat .iso-build/archiso/profile/airootfs/root/.automated_script.sh
          echo "=== END ==="
  
      ## rebuild the arch iso with custom script
      - name: Build Arch ISO with mkarchiso
        run: |
          set -euo pipefail

          BUILD_ROOT=".iso-build/archiso"
          mkdir -p "${BUILD_ROOT}/out"

          HOST_UID=$(id -u)
          HOST_GID=$(id -g)

          docker run --rm -i --privileged \
            -v "$PWD:/work" -w /work \
            -e ARCHISO_TRACE=1 \
            -e HOST_UID="$HOST_UID" -e HOST_GID="$HOST_GID" \
            archlinux:latest bash -lc '
              set -eux
              pacman -Syu --noconfirm archiso

              rm -rf .iso-build/archiso/out/*
              mkarchiso -v \
                -w /tmp/archiso-work \
                -o .iso-build/archiso/out \
                .iso-build/archiso/profile \
                2>&1 | tee .iso-build/archiso/out/build.log

              chown -R "$HOST_UID:$HOST_GID" .iso-build/archiso/out

              ls -lh .iso-build/archiso/out/*.iso
            '

      ## generate a build manifest
      - name: Generate ISO build manifest
        run: |
          set -euo pipefail

          ISO_PATH=$(echo .iso-build/archiso/out/*.iso)
          ISO_NAME=$(basename "$ISO_PATH")
          BASE_NAME="${ISO_NAME%.iso}"
          MANIFEST_PATH="infra/arch/artifacts/${BASE_NAME}.json"

          mkdir -p infra/arch/artifacts

          cat > "$MANIFEST_PATH" << 'EOF'
          {
            "iso_name": "__ISO_NAME__",
            "built_at": "__BUILT_AT__",
            "git_sha": "__GIT_SHA__",
            "proxmox_node": "__PVE_NODE__",
            "proxmox_storage": "__PVE_ISO_STORAGE__",
            "uploader_host": "__HOSTNAME__",
            "workflow_run_id": "__RUN_ID__"
          }
          EOF

          sed -i "s|__ISO_NAME__|${ISO_NAME}|g" "$MANIFEST_PATH"
          sed -i "s|__BUILT_AT__|$(date -u +"%Y-%m-%dT%H:%M:%SZ")|g" "$MANIFEST_PATH"
          sed -i "s|__GIT_SHA__|${GITHUB_SHA}|g" "$MANIFEST_PATH"
          sed -i "s|__PVE_NODE__|${PVE_NODE}|g" "$MANIFEST_PATH"
          sed -i "s|__PVE_ISO_STORAGE__|${PVE_ISO_STORAGE}|g" "$MANIFEST_PATH"
          sed -i "s|__HOSTNAME__|$(hostname)|g" "$MANIFEST_PATH"
          sed -i "s|__RUN_ID__|${GITHUB_RUN_ID}|g" "$MANIFEST_PATH"

          echo "Wrote manifest to: $MANIFEST_PATH"
          cat "$MANIFEST_PATH"

      ## upload custom iso to proxmox
      - name: Upload ISO to Proxmox
        run: |
          set -euo pipefail

          ISO_PATH=$(echo .iso-build/archiso/out/*.iso)
          ISO_NAME=$(basename "$ISO_PATH")

          BASE_URL="${PVE_ACCESS_HOST%/}"

          echo "Found ISO: $ISO_NAME"
          echo "Uploading to Proxmox storage: ${PVE_ISO_STORAGE} via ${BASE_URL}"

          curl -k --fail-with-body \
            -X POST "${BASE_URL}/api2/json/nodes/${PVE_NODE}/storage/${PVE_ISO_STORAGE}/upload?content=iso" \
            -H "Authorization: PVEAPIToken=${PM_TOKEN_ID}=${PM_TOKEN_SECRET}" \
            -F "filename=@${ISO_PATH}" \
            --progress-bar | tee upload-response.json

          echo "=== Upload response ==="
          cat upload-response.json

      ## update stable iso manifest
      - name: Update stable ISO manifest (optional)
        if: ${{ inputs.update-stable == 'yes' }}
        run: |
          set -euo pipefail

          ISO_PATH=$(echo .iso-build/archiso/out/*.iso)
          ISO_NAME=$(basename "$ISO_PATH")
          BASE_NAME="${ISO_NAME%.iso}"

          MANIFEST_SOURCE="infra/arch/artifacts/${BASE_NAME}.json"
          MANIFEST_STABLE="infra/arch/spec/iso-manifest-stable.json"

          echo "update-stable=true → updating ${MANIFEST_STABLE}"

          mkdir -p infra/arch/spec
          cp "$MANIFEST_SOURCE" "$MANIFEST_STABLE"

          echo "=== Stable manifest updated ==="
          cat "$MANIFEST_STABLE"

      ## commit iso manifest
      - name: Commit ISO manifests
        env:
          GIT_AUTHOR_NAME: github-actions[bot]
          GIT_AUTHOR_EMAIL: github-actions[bot]@users.noreply.github.com
          GIT_COMMITTER_NAME: github-actions[bot]
          GIT_COMMITTER_EMAIL: github-actions[bot]@users.noreply.github.com
        run: |
          set -euo pipefail

          git status --short

          # Stage all manifest JSONs and the stable pointer (if it exists)
          git add infra/arch/artifacts/*.json || true
          git add infra/arch/spec/iso-manifest-stable.json || true

          # Only commit if there is something staged
          if git diff --cached --quiet; then
            echo "No manifest changes to commit."
          else
            git commit -m "chore(iso): update ISO manifests"
            git push origin "${GITHUB_REF_NAME}"
          fi
