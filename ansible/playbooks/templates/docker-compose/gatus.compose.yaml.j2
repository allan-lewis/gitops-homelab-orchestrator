services:

  gatus:
    image: "twinproduction/gatus:latest"
    container_name: "gatus"
    restart: "unless-stopped"
    ports: 
      - "8080:8080/tcp"
    volumes: 
      - "/var/lib/gatus:/data"
      - "/etc/gatus:/config"
